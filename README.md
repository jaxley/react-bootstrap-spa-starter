ReactJS + Bootstrap Single Page Application starter template

Uses a sidebar for navigation.

## Setup

`yarn`

## Running

`yarn run start-dev`

Then, connect to http://localhost:3000 to view the app
